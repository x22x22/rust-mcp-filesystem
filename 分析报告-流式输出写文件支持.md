# rust-mcp-filesystem 流式输出写文件支持分析报告

## 项目概述

rust-mcp-filesystem 是一个基于 Rust 实现的高性能、异步、轻量级的 MCP (Model Context Protocol) 服务器，专注于高效处理各种文件系统操作。该项目是对 JavaScript 版本 `@modelcontextprotocol/server-filesystem` 的纯 Rust 重写，提供了增强的功能、改进的性能和健壮的特性集。

## 当前写文件实现分析

### 1. 现有实现方式

当前 `write_file` 工具的实现位于以下文件：

- **工具定义**: `src/tools/write_file.rs`
- **服务实现**: `src/fs_service/io/write.rs`

#### 核心代码流程：

```rust
// src/tools/write_file.rs
pub async fn run_tool(
    params: Self,
    context: &FileSystemService,
) -> std::result::Result<CallToolResult, CallToolError> {
    context
        .write_file(Path::new(&params.path), &params.content)
        .await
        .map_err(CallToolError::new)?;

    Ok(CallToolResult::text_content(vec![TextContent::from(
        format!("Successfully wrote to {}", &params.path),
    )]))
}

// src/fs_service/io/write.rs
pub async fn write_file(&self, file_path: &Path, content: &String) -> ServiceResult<()> {
    let allowed_directories = self.allowed_directories().await;
    let valid_path = self.validate_path(file_path, allowed_directories)?;
    tokio::fs::write(valid_path, content).await?;
    Ok(())
}
```

#### 当前实现特点：

1. **一次性写入**: 使用 `tokio::fs::write()` 一次性将整个内容写入文件
2. **单一响应**: 写入完成后返回一个简单的成功消息
3. **无进度反馈**: 在写入过程中没有任何中间进度通知
4. **同步返回**: 虽然使用了异步 I/O，但对客户端来说是一个完整的响应

## 可行性分析

### 1. 流式输出可行性

#### 技术层面：

**优势**：
- ✅ Rust 本身对流式 I/O 有强大的支持（通过 `tokio::io::AsyncWrite` 等）
- ✅ 项目已经使用 tokio 异步运行时，具备异步流处理基础
- ✅ 在 `src/fs_service/io/read.rs` 中已有流式处理的示例：
  ```rust
  use futures::{StreamExt, stream};
  let results = stream::iter(paths)
      .map(|path| async move { ... })
      .buffer_unordered(max_concurrent)
      .collect::<Vec<_>>()
      .await;
  ```

**挑战**：
- ❌ **MCP 协议限制**: MCP (Model Context Protocol) 协议基于 JSON-RPC 2.0，工具调用遵循请求-响应模式
- ❌ **SDK 限制**: `rust-mcp-sdk` (v0.8.1) 的 `CallToolResult` 类型仅支持一次性返回结果，不支持流式响应
- ❌ **协议设计**: 从 `src/handler.rs` 和 `src/server.rs` 可以看出，所有工具调用都返回单个 `CallToolResult`

#### MCP 协议架构分析：

```rust
// src/handler.rs 中的工具调用处理
async fn handle_call_tool_request(
    &self,
    params: CallToolRequestParams,
    _: Arc<dyn McpServer>,
) -> std::result::Result<CallToolResult, CallToolError> {
    // 返回单个 CallToolResult
}
```

MCP 协议中的通信模式：
- **工具调用 (Tools)**: 请求-响应模式，返回 `CallToolResult`
- **通知 (Notifications)**: 单向消息，但不适用于工具响应
- **资源 (Resources)**: 用于读取数据，但当前项目未启用
- **日志 (Logging)**: 可用于发送消息到 stderr，但不是标准的工具响应通道

### 2. 行级输出可行性

#### 实现方案：

**方案 A: 在单个响应中包含行级信息**

可行度：⭐⭐⭐⭐⭐ (高度可行)

实现方式：
```rust
pub async fn write_file_with_line_info(
    &self, 
    file_path: &Path, 
    content: &String
) -> ServiceResult<String> {
    let allowed_directories = self.allowed_directories().await;
    let valid_path = self.validate_path(file_path, allowed_directories)?;
    
    let lines: Vec<&str> = content.lines().collect();
    let total_lines = lines.len();
    let bytes = content.as_bytes().len();
    
    tokio::fs::write(valid_path, content).await?;
    
    Ok(format!(
        "Successfully wrote to {}\n\
         Lines written: {}\n\
         Bytes written: {}\n\
         First line: {}\n\
         Last line: {}",
        file_path.display(),
        total_lines,
        bytes,
        lines.first().unwrap_or(&""),
        lines.last().unwrap_or(&"")
    ))
}
```

**方案 B: 使用 stderr 消息进行进度通知**

可行度：⭐⭐⭐⭐ (可行但有限制)

从代码中可以看到，MCP 服务器支持通过 `runtime.stderr_message()` 发送日志消息：

```rust
// src/handler.rs 中的示例
let _ = runtime.stderr_message("Client does not support MCP Roots...").await;
```

可以在写入过程中发送进度消息：
```rust
pub async fn write_file_with_progress(
    &self,
    file_path: &Path,
    content: &String,
    runtime: Arc<dyn McpServer>,
) -> ServiceResult<()> {
    let allowed_directories = self.allowed_directories().await;
    let valid_path = self.validate_path(file_path, allowed_directories)?;
    
    let lines: Vec<&str> = content.lines().collect();
    let total_lines = lines.len();
    
    // 发送开始消息
    let _ = runtime.stderr_message(
        format!("Starting to write {} lines to {}", total_lines, file_path.display())
    ).await;
    
    // 写入文件
    tokio::fs::write(valid_path, content).await?;
    
    // 发送完成消息
    let _ = runtime.stderr_message(
        format!("Completed writing {} lines to {}", total_lines, file_path.display())
    ).await;
    
    Ok(())
}
```

**限制**：
- stderr 消息主要用于调试和诊断信息
- 不是工具响应的标准输出通道
- 客户端可能不会显示或可能会以不同方式显示这些消息

**方案 C: 分块写入并报告进度**

可行度：⭐⭐⭐ (技术可行但架构不支持)

对于大文件，可以实现分块写入：
```rust
use tokio::io::AsyncWriteExt;

pub async fn write_file_chunked(
    &self,
    file_path: &Path,
    content: &String,
    runtime: Arc<dyn McpServer>,
) -> ServiceResult<()> {
    let allowed_directories = self.allowed_directories().await;
    let valid_path = self.validate_path(file_path, allowed_directories)?;
    
    let mut file = tokio::fs::File::create(valid_path).await?;
    let lines: Vec<&str> = content.lines().collect();
    let total_lines = lines.len();
    
    for (idx, line) in lines.iter().enumerate() {
        file.write_all(line.as_bytes()).await?;
        file.write_all(b"\n").await?;
        
        // 每 100 行报告一次进度
        if (idx + 1) % 100 == 0 {
            let _ = runtime.stderr_message(
                format!("Progress: {}/{} lines written", idx + 1, total_lines)
            ).await;
        }
    }
    
    file.flush().await?;
    Ok(())
}
```

## 技术限制总结

### MCP 协议限制

1. **请求-响应模式**: MCP 协议基于 JSON-RPC 2.0，采用严格的请求-响应模式
2. **无原生流式支持**: 协议规范中没有为工具调用定义流式响应机制
3. **单一响应对象**: 每个工具调用必须返回一个完整的 `CallToolResult`

### SDK 架构限制

1. **CallToolResult 结构**: 
   ```rust
   pub struct CallToolResult {
       pub content: Vec<Content>,
       pub is_error: Option<bool>,
       pub meta: Option<HashMap<String, Value>>,
   }
   ```
   这个结构设计用于单次响应，不支持多次返回

2. **处理器接口**: `ServerHandler` trait 中的 `handle_call_tool_request` 方法返回单个 `Result<CallToolResult, CallToolError>`

3. **无进度报告机制**: SDK 没有提供标准的进度报告 API

## 推荐实现方案

### 方案 1: 增强响应内容（推荐 ⭐⭐⭐⭐⭐）

**实现内容**：
在写文件成功后，返回包含详细信息的响应，包括：
- 写入的总行数
- 写入的总字节数
- 文件的前几行和后几行预览
- 写入时间戳
- 文件路径

**优势**：
- ✅ 完全符合 MCP 协议规范
- ✅ 不需要修改 SDK 或协议
- ✅ 实现简单，风险低
- ✅ 客户端兼容性好

**示例代码**：
```rust
pub async fn write_file_verbose(
    &self, 
    file_path: &Path, 
    content: &String
) -> ServiceResult<String> {
    let start_time = std::time::Instant::now();
    let allowed_directories = self.allowed_directories().await;
    let valid_path = self.validate_path(file_path, allowed_directories)?;
    
    // 分析内容
    let lines: Vec<&str> = content.lines().collect();
    let total_lines = lines.len();
    let total_bytes = content.as_bytes().len();
    
    // 写入文件
    tokio::fs::write(valid_path, content).await?;
    
    let elapsed = start_time.elapsed();
    
    // 构建详细响应
    let mut response = format!(
        "Successfully wrote to {}\n\n\
         Statistics:\n\
         - Total lines: {}\n\
         - Total bytes: {}\n\
         - Time elapsed: {:.2}ms\n",
        file_path.display(),
        total_lines,
        total_bytes,
        elapsed.as_secs_f64() * 1000.0
    );
    
    // 添加内容预览
    if total_lines > 0 {
        response.push_str("\nContent preview:\n");
        
        // 前5行
        let preview_lines = std::cmp::min(5, total_lines);
        response.push_str(&format!("First {} lines:\n", preview_lines));
        for (i, line) in lines.iter().take(preview_lines).enumerate() {
            response.push_str(&format!("  {}: {}\n", i + 1, line));
        }
        
        // 如果文件超过10行，显示最后5行
        if total_lines > 10 {
            response.push_str(&format!("\n... ({} lines omitted) ...\n\n", total_lines - 10));
            response.push_str("Last 5 lines:\n");
            for (i, line) in lines.iter().rev().take(5).rev().enumerate() {
                let line_num = total_lines - 4 + i;
                response.push_str(&format!("  {}: {}\n", line_num, line));
            }
        }
    }
    
    Ok(response)
}
```

### 方案 2: 使用 stderr 进度通知（辅助方案 ⭐⭐⭐）

**实现内容**：
对于大文件写入，在写入过程中通过 stderr 发送进度消息。

**使用场景**：
- 写入超大文件（> 10MB）
- 需要让用户知道操作仍在进行中
- 调试和监控

**限制**：
- stderr 消息可能不会显示在某些客户端中
- 不是标准的工具响应通道
- 主要用于日志和诊断

### 方案 3: 添加 write_file_verbose 新工具（备选方案 ⭐⭐⭐⭐）

**实现内容**：
保留现有的 `write_file` 工具以保持向后兼容性，添加新的 `write_file_verbose` 或 `write_file_with_stats` 工具，提供详细的写入信息。

**优势**：
- ✅ 向后兼容
- ✅ 用户可以选择使用简单或详细版本
- ✅ 不影响现有客户端

## 参考：现有类似实现

项目中的 `edit_file` 工具提供了一个良好的参考示例，它返回详细的 diff 信息：

```rust
// src/tools/edit_file.rs
pub async fn run_tool(
    params: Self,
    context: &FileSystemService,
) -> std::result::Result<CallToolResult, CallToolError> {
    let diff = context
        .apply_file_edits(Path::new(&params.path), params.edits, params.dry_run, None)
        .await
        .map_err(CallToolError::new)?;

    Ok(CallToolResult::text_content(vec![TextContent::from(diff)]))
}
```

这个工具在完成编辑后返回完整的 diff 信息，展示了如何在单个响应中提供详细的操作结果。

## SDK 版本升级分析

### 当前版本 vs 最新版本

**当前使用版本**: `rust-mcp-sdk v0.8.1`
**最新可用版本**: `rust-mcp-sdk v0.8.3` (2026年2月发布)

### 最新版本新特性

根据对 rust-mcp-sdk v0.8.3 的调研，发现 SDK 已经支持以下特性：

1. **进度通知 (Progress Notifications)**
   - MCP 协议 2025-11-25 版本原生支持进度通知
   - SDK 提供 `notify_progress()` 方法
   - 可以在工具执行过程中发送进度更新

2. **Progress Notification 结构**
   ```rust
   pub struct ProgressNotificationParams {
       // 描述当前进度的可选消息
       pub message: Option<String>,
       // 进度值 (0.0 到 1.0 或其他约定的范围)
       pub progress: f64,
       // 元数据
       pub meta: Option<Map<String, Value>>,
   }
   ```

3. **McpServer Trait 方法**
   ```rust
   async fn notify_progress(&self, params: ProgressNotificationParams) -> SdkResult<()>
   ```

### 实现方案：使用进度通知

升级到 v0.8.3 后，可以在写文件过程中发送进度通知：

```rust
use rust_mcp_sdk::schema::ProgressNotificationParams;

impl WriteFile {
    pub async fn run_tool(
        params: Self,
        context: &FileSystemService,
        runtime: Arc<dyn McpServer>,  // 新增参数
    ) -> std::result::Result<CallToolResult, CallToolError> {
        let content = &params.content;
        let lines: Vec<&str> = content.lines().collect();
        let total_lines = lines.len();
        
        // 发送开始通知
        let _ = runtime.notify_progress(ProgressNotificationParams {
            message: Some(format!("开始写入 {} 行到 {}", total_lines, params.path)),
            progress: 0.0,
            meta: None,
        }).await;
        
        // 执行写入（可以分块处理）
        let file_path = Path::new(&params.path);
        
        // 如果是大文件，可以分块写入并报告进度
        if total_lines > 100 {
            let mut file = tokio::fs::File::create(file_path).await?;
            for (idx, line) in lines.iter().enumerate() {
                file.write_all(line.as_bytes()).await?;
                file.write_all(b"\n").await?;
                
                // 每 10% 报告一次进度
                if (idx + 1) % (total_lines / 10).max(1) == 0 {
                    let progress = (idx + 1) as f64 / total_lines as f64;
                    let _ = runtime.notify_progress(ProgressNotificationParams {
                        message: Some(format!("已写入 {}/{} 行", idx + 1, total_lines)),
                        progress,
                        meta: None,
                    }).await;
                }
            }
            file.flush().await?;
        } else {
            // 小文件直接写入
            context.write_file(file_path, content).await?;
        }
        
        // 发送完成通知
        let _ = runtime.notify_progress(ProgressNotificationParams {
            message: Some(format!("完成写入 {} 行", total_lines)),
            progress: 1.0,
            meta: None,
        }).await;
        
        Ok(CallToolResult::text_content(vec![TextContent::from(
            format!("成功写入 {} 行到 {}", total_lines, params.path),
        )]))
    }
}
```

## 结论

### 流式输出

**更新后的结论**: ✅ **升级到 rust-mcp-sdk v0.8.3 后可以实现进度通知**

虽然工具调用仍然是请求-响应模式（最终返回单个 `CallToolResult`），但 MCP 协议 2025-11-25 版本和 rust-mcp-sdk v0.8.3+ 已经支持：

1. **进度通知 (Progress Notifications)**: 可以在工具执行过程中发送多个进度更新通知
2. **非阻塞通知**: 进度通知是单向的，不需要等待响应
3. **标准化接口**: 通过 `notify_progress()` 方法发送，客户端可以标准化处理

**关键变化**：
- ❌ ~~"不支持真正的流式输出"~~ 
- ✅ **支持进度通知，可以在写入过程中实时报告进度**
- ✅ **工具最终仍返回单个结果，但过程可以有多次进度更新**

这意味着：
- 可以在写入大文件时报告进度（已写入 X/Y 行）
- 客户端可以显示进度条或状态更新
- 不需要修改 MCP 协议本身

### 行级输出

**结论**: ✅ **可以实现，推荐"增强响应内容"方案**

虽然不能在写入过程中实时输出每一行，但可以在写入完成后返回包含详细行级信息的响应，包括：
- 总行数统计
- 内容预览（前几行和后几行）
- 字节数统计
- 执行时间
- 其他元数据

这种方式：
- ✅ 完全符合 MCP 协议规范
- ✅ 实现简单，风险低
- ✅ 不需要修改协议或 SDK
- ✅ 能够为用户提供有价值的写入操作详细信息

## 升级建议

### 立即可行的方案（推荐 ⭐⭐⭐⭐⭐）

**升级到 rust-mcp-sdk v0.8.3 并实现进度通知**

**实施步骤**：

1. **更新 Cargo.toml**
   ```toml
   [dependencies]
   rust-mcp-sdk = {version="0.8.3", default-features = false, features = [
       "server",
       "macros",
       "stdio"
   ] }
   ```

2. **修改 write_file 工具签名**
   - 在 `run_tool` 方法中添加 `runtime: Arc<dyn McpServer>` 参数
   - 在写入过程中调用 `runtime.notify_progress()` 发送进度更新

3. **更新 handler.rs 中的工具调用**
   - 将 `runtime` 参数传递给工具的 `run_tool` 方法

**优势**：
- ✅ 使用 MCP 协议标准特性，无需 hack
- ✅ 客户端可以标准化显示进度（进度条、百分比等）
- ✅ 适用于所有支持 MCP 2025-11-25 协议的客户端
- ✅ 代码改动最小，向后兼容性好
- ✅ 性能开销小（只在需要时发送通知）

### 其他建议

1. **短期建议**: 同时实现"增强响应内容"方案，在最终响应中提供详细统计信息

2. **中期建议**: 为大文件（如 > 1000 行）启用进度通知，小文件保持简单响应

3. **长期建议**: 监控 MCP 协议演进，如有更好的流式支持特性，及时采用

## 参考文档

- MCP 协议规范: https://modelcontextprotocol.io/
- rust-mcp-sdk: https://github.com/rust-mcp-stack/rust-mcp-sdk
- JSON-RPC 2.0 规范: https://www.jsonrpc.org/specification
